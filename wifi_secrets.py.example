"""
Copy this file to wifi_secrets.py (gitignored -- see .gitignore) and fill in real credentials.
ntp_clock.py imports wifi_secrets for these two constants; keeping them in a separate,
uncommitted file is the same pattern this repo already uses to keep the confidential
Datasheet/ folder out of git.
"""

SSID = "your-wifi-ssid"
PASSWORD = "your-wifi-password"
